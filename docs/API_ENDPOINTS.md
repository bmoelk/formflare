# FormFlare API Endpoints Reference 📡

This document provides complete, detailed specifications for all REST API endpoints exposed by the FormFlare Worker.

---

## 📑 Summary sitemap

| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :--- |
| `GET` | [`/`](#get-) | Service health check & metadata | None |
| `GET` | [`/form-handler.js`](#get-form-handlerjs) | FormFlare Client JavaScript library script | None |
| `POST` | [`/submit`](#post-submit) | Ingest form submission with Turnstile validation | None (Turnstile token) |
| `GET` | [`/submissions/:formId`](#get-submissionsformid) | List paginated submissions for a specific form | `Bearer API_KEY` |
| `GET` | [`/submission/:id`](#get-submissionid) | Retrieve details for a single submission by ID | `Bearer API_KEY` |
| `POST` | [`/email-test`](#post-email-test) | Trigger test email dispatch to verify provider | `Bearer API_KEY` |

---

## 1. Public Endpoints

### `GET /`

Returns service health, version info, and current server timestamp.

#### Request
```http
GET / HTTP/1.1
Host: your-worker.workers.dev
```

#### Response (`200 OK`)
```json
{
  "service": "FormFlare",
  "version": "1.0.0",
  "status": "healthy",
  "timestamp": "2026-08-18T14:23:52.000Z"
}
```

---

### `GET /form-handler.js`

Serves the built-in FormFlare client JavaScript library file with proper `Content-Type: application/javascript`, CORS headers, and edge caching (`Cache-Control: public, max-age=3600`).

#### Request
```html
<script src="https://your-worker.workers.dev/form-handler.js"></script>
```

---

### `POST /submit`

Ingests form data from client forms, verifies anti-spam Turnstile tokens, persists submissions to KV / D1 storage, and triggers outbound email alerts or webhooks.

#### Headers
```http
Content-Type: application/json
```

#### Request Payload
```json
{
  "formId": "contact",
  "siteId": "mysite",
  "turnstileToken": "0.1x000000000000000000...",
  "data": {
    "name": "Jane Doe",
    "email": "jane@example.com",
    "message": "Hello from FormFlare!"
  }
}
```

#### Field Specifications
* `formId` *(string, required)*: Identifier for the form (e.g., `contact`, `newsletter`).
* `siteId` *(string, required)*: Explicit website identifier (e.g., `splitphase`, `brainendeavor`). Used to partition storage keys and route to site-specific secrets and email alert recipients.
* `turnstileToken` *(string, required)*: Token generated by the Cloudflare Turnstile widget.
* `data` *(object, required)*: Key-value dictionary containing form field inputs.

#### Success Response (`200 OK`)
```json
{
  "success": true,
  "submissionId": "f8a92b10-c3d4-4e5f-9a1b-2c3d4e5f6a7b",
  "message": "Form submitted successfully"
}
```

#### Error Responses

##### `400 Bad Request` (Missing fields)
```json
{
  "success": false,
  "error": "Site ID is required. Please provide 'siteId' in the payload or specify 'data-formflare-site' on your form."
}
```

##### `403 Forbidden` (Turnstile failure)
```json
{
  "success": false,
  "error": "Turnstile verification failed",
  "details": ["invalid-input-response"]
}
```

##### `429 Too Many Requests` (Rate limit exceeded)
```json
{
  "success": false,
  "error": "Rate limit exceeded",
  "retryAfter": 45
}
```

---

## 2. Authenticated Admin Endpoints

All admin endpoints require an `Authorization` HTTP header with a Bearer token matching your `API_KEY` worker secret:

```http
Authorization: Bearer YOUR_API_KEY
```

---

### `GET /submissions/:formId`

Lists historical form submissions stored for a specific `formId` and `siteId`. Requires the `?siteId=` query parameter, with optional pagination via `limit` and `offset`.

#### Request
```http
GET /submissions/contact_form?siteId=brainendeavor&limit=50&offset=0 HTTP/1.1
Host: your-worker.workers.dev
Authorization: Bearer YOUR_API_KEY
```

#### Response (`200 OK`)
```json
{
  "success": true,
  "formId": "contact_form",
  "siteId": "brainendeavor",
  "submissions": [
    {
      "id": "f8a92b10-c3d4-4e5f-9a1b-2c3d4e5f6a7b",
      "formId": "contact_form",
      "siteId": "brainendeavor",
      "data": {
        "name": "Jane Doe",
        "email": "jane@example.com",
        "message": "Hello from FormFlare!"
      },
      "metadata": {
        "ip": "203.0.113.195",
        "userAgent": "Mozilla/5.0...",
        "timestamp": "2026-08-18T14:23:52.000Z",
        "turnstileScore": 1.0
      }
    }
  ],
  "pagination": {
    "limit": 50,
    "offset": 0
  }
}
```

---

### `GET /submission/:id`

Retrieves full metadata and payload details for a single submission by its unique submission ID. Optional `?siteId=` and `?formId=` parameters can be provided for instant indexed lookup.

#### Request
```http
GET /submission/f8a92b10-c3d4-4e5f-9a1b-2c3d4e5f6a7b?siteId=brainendeavor&formId=contact_form HTTP/1.1
Host: your-worker.workers.dev
Authorization: Bearer YOUR_API_KEY
```

#### Response (`200 OK`)
```json
{
  "success": true,
  "submission": {
    "id": "f8a92b10-c3d4-4e5f-9a1b-2c3d4e5f6a7b",
    "formId": "contact_form",
    "data": {
      "name": "Jane Doe",
      "email": "jane@example.com",
      "message": "Hello from FormFlare!"
    },
    "metadata": {
      "ip": "203.0.113.195",
      "userAgent": "Mozilla/5.0...",
      "timestamp": "2026-08-18T14:23:52.000Z",
      "turnstileScore": 1.0
    }
  }
}
```

---

### `POST /email-test`

Dispatches a test email notification to verify outbound email integration (Resend, SendGrid, Mailgun, Mailtrap, or Console).

#### Request
```http
POST /email-test HTTP/1.1
Host: your-worker.workers.dev
Authorization: Bearer YOUR_API_KEY
```

#### Response (`200 OK`)
```json
{
  "success": true,
  "message": "Test email sent successfully",
  "provider": "resend"
}
```
